{
    "name": "run_query",
    "description": "Runs SQL query against a ClickHouse db.",
    "parameters": {
        "type": "object",
        "properties": {
            "sql_query": {
                "type": "string",
                "description": "SQL query to run"
            }
        },
        "required": ["sql_query"]
    }
}
