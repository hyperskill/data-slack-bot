{
    "name": "send_user_request",
    "description": "Sends user request about SQL query to a colleague",
    "parameters": {
        "type": "object",
        "properties": {
            "user_request": {
                "type": "string",
                "description": "User request to send"
            }
        },
        "required": ["user_request"]
    }
}
